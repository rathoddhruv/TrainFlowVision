FROM node:20-alpine AS build
WORKDIR /app
COPY FE/package*.json ./
RUN npm ci
COPY FE/ ./
RUN npm run build --prod

FROM nginx:alpine
COPY --from=build /app/dist/trainflow-vision/browser /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
